@{
    ViewBag.Title = "Home Page";
}

<h2>@ViewBag.Message</h2>
<h2>@ViewData["Message"]</h2>

<ul>
@foreach (var s in @ViewBag.Colors)
{
    <li>@s</li>
}
</ul>
<ul>
@foreach (var s in (List<string>)@ViewData["Colors"])
{
    <li>@s</li>
}
</ul>